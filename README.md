# 🐚 Minishell

A minimal shell implementation in C, inspired by **bash**.  
Supports basic command execution, I/O redirection, pipelines, and built-in commands.

---

## ⚙️ Requirements

You need:
- **GNU Make**
- **GCC (GNU Compiler Collection)**
- A **POSIX-compliant system** (Linux or macOS recommended)

---

## 🧩 Build Instructions

Clone the repository and build the project:
```bash
git clone https://github.com/juantorrero10/minishell.git
cd minishell
make

